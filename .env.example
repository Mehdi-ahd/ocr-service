OCR_PORT=8001
OCR_TOKEN=
OCR_LANGS=fra+eng
# CORS — * en dev, restreindre en prod (ex: https://ton-site.infinityfreeapp.com)
OCR_CORS_ORIGINS=*

# Render injecte PORT automatiquement — ne pas définir en prod
# PORT=8000
